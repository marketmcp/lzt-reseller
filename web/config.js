/* ════════════════════════════════════════════════════════════════════════
   VAULT — Reseller Configuration
   ════════════════════════════════════════════════════════════════════════
   Это ЕДИНСТВЕННЫЙ файл, который нужно править для полного редизайна магазина.
   Всё ниже меняется без костылей — каждый параметр привязан к дизайн-токену.

   Можно править вручную или попросить Claude (через MCP), например:
     «Сделай тему ocean, бренд назови CyberStore, акцент сделай #ff6600»
     «Поставь светлую тему, скругления rounded, плотность spacious»
     «Покажи только Steam и Discord, наценка 20%, hero-заголовок свой»
     «Перекрась полностью: фон #0e0f1a, акцент #00e0b0, текст #eaf2ff»
   ════════════════════════════════════════════════════════════════════════ */

window.GHOSTSHOP_CONFIG = {

  /* ─────────────────────────── БРЕНД ─────────────────────────── */
  brand: {
    name: 'Vault',
    tagline: 'Маркетплейс цифровых аккаунтов',
    logoImage: null,          // 'assets/logo.svg' — заменяет текст+знак логотипа
    telegram: '#',            // 'https://t.me/yourshop'
    discord:  '#',            // 'https://discord.gg/yourshop'
    vk:       '#',            // 'https://vk.com/yourshop'
    support:  '#',
  },

  /* ─────────────────────────── ТЕМА ──────────────────────────────
     preset — готовая палитра. colors — переопределяет ЛЮБОЙ токен
     поверх пресета (оставьте null, чтобы взять из пресета).
     ────────────────────────────────────────────────────────────── */
  theme: {
    // 'vault' | 'obsidian' | 'ocean' | 'ember' | 'matrix' | 'rose' | 'light'
    preset: 'vault',

    // Полное переопределение палитры (любой ключ — null = из пресета):
    colors: {
      bg:            null,   // фон страницы (самый тёмный слой)
      surface1:      null,   // панели: навбар, строки, модалки
      surface2:      null,   // поля ввода, ховер-поверхности
      surface3:      null,   // чипы, тумблеры
      border:        null,   // тонкая граница
      borderStrong:  null,   // сильная граница / фокус
      text:          null,   // основной текст
      textMuted:     null,   // приглушённый текст
      textFaint:     null,   // подписи
      accent:        null,   // акцент (кнопки, активное, цены) — напр. '#ff6600'
      accentDeep:    null,   // тёмный акцент (авто из accent если null)
      accentLight:   null,   // светлый акцент (авто из accent если null)
      accentContrast:null,   // текст на акценте (авто #fff/#000 если null)
      success:       null,   // '#2bd07a'
      danger:        null,   // '#f0556b'
      warning:       null,   // '#f5a623'
      info:          null,   // '#4aa3ff'
    },

    radius:  'normal',   // 'sharp' | 'normal' | 'rounded' | число (px)
    density: 'normal',   // 'compact' | 'normal' | 'spacious'
    shadow:  'soft',     // 'flat' | 'soft' | 'deep'

    font: {
      body:    null,     // напр. "'Manrope',sans-serif"
      display: null,     // шрифт заголовков/логотипа
      url:     null,     // ссылка на @font-face CSS (Google Fonts и т.п.)
    },
  },

  /* ─────────────────────────── HERO-БЛОК ─────────────────────────
     По умолчанию ВЫКЛЮЧЕН — магазин сразу ведёт в каталог, как
     настоящий маркетплейс (без маркетинговой «воды»).
     enabled:true — включить лендинг-баннер с картинкой и текстом.
     image:null — без картинки (только токен-градиент под акцент).
     headline — HTML (<em> подсвечивается акцентом).
     ────────────────────────────────────────────────────────────── */
  hero: {
    enabled: false,
    image:   'assets/hero.png',   // 'assets/hero-alt.png' — второй вариант · null — без картинки
    eyebrow: 'Маркетплейс цифровых аккаунтов',
    headline:'Тысячи аккаунтов.<br><em>Мгновенная</em> выдача.',
    sub:     'Steam, Discord, игровые и соцсети — с автоматической доставкой данных и гарантией замены. Покупай безопасно за пару кликов.',
    ctaText: 'Открыть каталог',
    stats: [
      { v: '12 480',  l: 'аккаунтов в наличии' },
      { v: '37 200+', l: 'успешных сделок' },
      { v: '4.9 ★',   l: 'средний рейтинг' },
      { v: '24/7',    l: 'поддержка' },
    ],
  },

  /* ──────────────────── TRUST-ПОЛОСА (преимущества) ───────────────
     icon: 'bolt' | 'shield' | 'lock' | 'headset'
     ────────────────────────────────────────────────────────────── */
  features: [
    { icon: 'bolt',    title: 'Мгновенная выдача', desc: 'Данные сразу после оплаты' },
    { icon: 'shield',  title: 'Гарантия замены',   desc: 'Не подошёл — заменим' },
    { icon: 'lock',    title: 'Безопасная оплата',  desc: 'Карта · USDT · СБП' },
    { icon: 'headset', title: 'Поддержка 24/7',     desc: 'Ответим за минуты' },
  ],

  /* ─────────────────────────── КАТЕГОРИИ ─────────────────────────
     null = все 29. Массив = только указанные.
     ID: steam vk instagram twitter facebook tiktok youtube twitch
     discord telegram fortnite valorant apex genshin eft roblox
     minecraft lol bnet epic ea ubisoft rockstar supercell wot
     warzone gifts vpn
     ────────────────────────────────────────────────────────────── */
  categories: null,

  // Переопределить акцентный цвет конкретных категорий (левый бордер/теги/цена):
  // { steam:'#1a9fff', discord:'#5865f2' }
  categoryColors: {},

  /* ─────────────────────────── ЦЕНА ──────────────────────────── */
  markup:   1.0,     // множитель: 1.0 без наценки, 1.2 = +20%
  minPrice: 0,       // скрыть дешевле (0 = нет лимита)
  maxPrice: 0,       // скрыть дороже  (0 = нет лимита)

  // Кастомный фильтр: (p) => bool. Поля: id,title,cat,price,oldPrice,tags[],det[{l,v}],si
  //   Только Steam с Prime: (p) => p.cat!=='steam' || p.det.some(d=>d.l==='Prime'&&d.v==='Да')
  productFilter: null,

  currency: 'RUB',   // RUB | USD | EUR

  /* ─────────────────── СПОСОБЫ ОПЛАТЫ / МЕРЧАНТЫ ─────────────────
     Полностью настраиваемо. Каждый метод — свой мерчант/шлюз.
     Подключайте своих провайдеров через MCP или в Админ-панели.
       id       — уникальный код
       name     — название для покупателя
       icon     — slug simpleicons (cdn.simpleicons.org) или null
       enabled  — показывать ли
       merchant — id мерчанта / кошелёк / shop_id вашего шлюза
       provider — 'card' | 'crypto' | 'sbp' | 'custom' (для бэкенда)
     ────────────────────────────────────────────────────────────── */
  payments: [
    // provider:'crypto' + CRYPTOBOT_TOKEN в .env = реальная оплата через CryptoBot.
    // Без токена этот метод работает как демо (списание с баланса).
    { id:'crypto', name:'Крипта (CryptoBot)', icon:'bitcoin', provider:'crypto', merchant:'', enabled:true },
    { id:'card',   name:'Карта', icon:'visa', provider:'card', merchant:'', enabled:true },
    { id:'sbp',    name:'СБП',   icon:null,    provider:'sbp',  merchant:'', enabled:true },
    // Пример своего мерчанта:
    // { id:'yoomoney', name:'ЮMoney', icon:'yoomoney', provider:'custom', merchant:'4100xxxxxxxx', enabled:true },
  ],

  /* ─────────────────────── УВЕДОМЛЕНИЯ ──────────────────────────
     Telegram-бот для оповещений администратора о событиях магазина.
     Настраивается через MCP или в Админ-панели → Уведомления.
     ────────────────────────────────────────────────────────────── */
  notifications: {
    telegram: {
      botToken: '',          // токен бота от @BotFather
      chatId:   '',          // ваш chat id / id канала
      events: {
        newOrder:  true,     // новый заказ
        newUser:   false,    // новый пользователь
        lowStock:  false,    // мало товара в категории
        payout:    true,     // выплаты/пополнения
      },
    },
  },

  /* ─────────────────────── АДМИН-ПАНЕЛЬ ─────────────────────────
     Доступна из меню пользователя. Все настройки можно менять и
     здесь (вручную), и через MCP — это один и тот же config.
     ────────────────────────────────────────────────────────────── */
  admin: {
    enabled: true,
    // Пароль владельца для входа в админ-панель (ссылка: ваш-сайт/#admin).
    // ⚠ ОБЯЗАТЕЛЬНО смените перед публикацией!
    password: 'owner',
  },
};

/* ─────────────────────── LIVE-РЕЖИМ (реальный LZT Market) ───────────────
   Демо показывает сид-данные. Чтобы подключить реальный маркет:
     1) добавьте <script src="lzt-api.js"></script> после основного скрипта
     2) вызовите:  LZT.goLive({ token: 'ВАШ_ТОКЕН', currency: 'rub',
                                categories: ['steam','discord','fortnite'] })
   Запросы идут через кеш + дедуп + ретраи (API_CACHE), с учётом лимитов
   LZT (120/мин общий, 20/мин на поиск) — тысячи заказов не дёргают API.
   ──────────────────────────────────────────────────────────────────────── */
